DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# Set to "true" when connecting to a managed Postgres provider that requires SSL
# (Render, Supabase, Neon, Railway, etc.)
DB_SSL=

JWT_SECRET=
JWT_EXPIRES_IN=7d

PORT=
FRONTEND_URL=

# Used once, by npm run seed:admin, to create the first admin account.
ADMIN_USERNAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=
